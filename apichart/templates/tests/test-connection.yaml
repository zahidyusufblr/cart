apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apichart.fullname" . }}-test-connection"
  labels:
{{ include "apichart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "apichart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
